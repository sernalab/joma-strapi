export const { VITE_API_URL } = import.meta.env;
export const { VITE_API_URL: API_URL } = import.meta.env;

export const BEARER = "Bearer";

export const ROLES = {
  DISTRIBUIDOR_A: "Distribuidor A",
  DISTRIBUIDOR_B: "Distribuidor B",
};
